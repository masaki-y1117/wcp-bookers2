<h1>Books</h1>
<table>
  <th>
    <tr>Title</tr>
    <tr>Opinion</tr>
  </th>
  <% @books.each do |book| %>
    <tr>
      <td><%= link_to book.title, book_path(book.id) %></td>
      <td><%= book.body %></td>
    </tr>
  <% end %>
</table>